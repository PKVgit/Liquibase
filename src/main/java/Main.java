
public class Main {
    public static  void main(String args []) {
        // Humans w = Humans.getWorker(1);
        // Humans.getInfoAboutWorker(1);
       Continents c = Continents.getCompany(1);
       Countries cou = Countries.getCountry(2);
       Cities dir = Cities.getDirectory(1);
       Groups of = Groups.getOffice(2);
       TypeOfCompany tof = TypeOfCompany.getType(1);

    }
}
